# Write a fuction to balance an unbalance binary search tree

from keyvalquiz import make_balanced_bst
from keyValue import all_list


def balance_bst(node):
    return make_balanced_bst(all_list(node))